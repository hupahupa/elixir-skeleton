#!/bin/bash

# Generated by Chef, do not edit

. <%= node[:skeleton][:python][:virtualenv] %>/bin/activate

set -e

bin_dir="$(cd "$(dirname "$0")/.."; pwd)"
cd "$bin_dir"

exec gunicorn -c scripts/gunicorn_config.py application:app

