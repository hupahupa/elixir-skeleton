# Generated by Chef, do not edit

description "Start Flask"
author "Hoang Xuan Phu <phu@cogini.com>"

<%- app_user = node[:skeleton][:app_user] -%>
<%- site_dir = node[:skeleton][:site_dir] -%>

start on runlevel [2345]
stop on runlevel [06]

exec su -s /bin/sh -c 'exec "$0" "$@"' <%= app_user %> -- <%= site_dir %>/scripts/run-foreground.sh
respawn
